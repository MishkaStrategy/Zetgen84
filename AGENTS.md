# AGENTS.md — MishkaStrategy/Zetgen84 AI bootstrap

## Organization AI policy

For non-trivial work in `MishkaStrategy/Zetgen84`, load the current `main` version of `MishkaStrategy/.github/AI_AGENT_POLICY.md`.

Repository-specific instructions in this repository take precedence over organization defaults where they conflict. Platform rules and the user's explicit current request remain higher priority.

GitHub is the source of truth for repository state unless this repository explicitly defines another canonical source.

Do not copy the full organization policy into this repository.